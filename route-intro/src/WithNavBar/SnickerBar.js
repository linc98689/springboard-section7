import React from 'react';
import img from "../images/snickers.png";

const SnickerBar = ()=>{
    return (
        <div className="background-image"  style={{backgroundImage: `url("${img}")`}}>
        </div>
    );
}

export default SnickerBar;
